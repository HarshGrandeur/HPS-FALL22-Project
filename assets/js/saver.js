//var $ = require('jQuery');
module.exports = function () {
    return{
        saveScore: function (playerName, score) {
            /*$.get( "../../dbman/saveScore.php",
                { gamename: "Auction Triplets 2", playername: playerName, score: score},
                function() {
            }).done(function() {
                
            }).fail(function() {
                alert("Score save error");
            });*/
        }
    }
};
